# Agent Note instructions

Active Karaka decisions use bilingual triplets under `implemented/`, `proposed/`, or `rejected/`. Historical pre-reset records live under `legacy/deepseek-harness/.agents/notes/`. Never edit, move, or delete `archived/`.
