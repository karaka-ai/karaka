# 防御性模式

[English](defensive-patterns.md) | 中文

插件释放必须达到静止状态：请求取消、等待所拥有的任务，并在延迟完成可能发布结果之前移除监听器。Loader 或 Include 更新失败时，必须保持最后一次接受的插件树处于活动状态。回调分发必须隔离单个观察者的失败，使后续观察者和所有权清理仍能执行。

这些规则保护本地修改的 Cordis fork 的生命周期行为。修改这些规则前必须添加针对性的回归测试。
