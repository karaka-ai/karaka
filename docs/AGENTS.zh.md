# 文档说明

每份活动文档均以英文和简体中文维护。英文文件使用 `.md`，中文文件使用 `.zh.md`，每个配对都具有同目录的 `.i18n.yaml` 一致性记录。同步更新两侧后运行 `pnpm run docs:record`，再运行 `pnpm run docs:check`。

活动文档描述 Karaka 当前的基础层。历史 harness 材料位于 `legacy/deepseek-harness/`，不参与活动验证。
